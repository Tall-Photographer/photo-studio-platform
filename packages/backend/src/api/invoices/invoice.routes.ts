// packages/backend/src/api/invoices/invoice.routes.ts
import { Router } from 'express';

const router = Router();

// Placeholder routes - will be implemented in next phase
router.get('/', (req, res) => {
  res.json({ message: 'Invoice routes - coming soon' });
});

export default router;