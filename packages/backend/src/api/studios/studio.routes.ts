// packages/backend/src/api/studios/studio.routes.ts
import { Router } from 'express';

const router = Router();

// Placeholder routes - will be implemented in next phase
router.get('/', (req, res) => {
  res.json({ message: 'Studio routes - coming soon' });
});

export default router;