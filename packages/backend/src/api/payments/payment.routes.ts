// packages/backend/src/api/payments/payment.routes.ts
import { Router } from 'express';

const router = Router();

// Placeholder routes - will be implemented in next phase
router.get('/', (req, res) => {
  res.json({ message: 'Payment routes - coming soon' });
});

export default router;