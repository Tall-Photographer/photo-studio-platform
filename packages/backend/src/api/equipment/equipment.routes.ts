// packages/backend/src/api/equipment/equipment.routes.ts
import { Router } from 'express';

const router = Router();

// Placeholder routes - will be implemented in next phase
router.get('/', (req, res) => {
  res.json({ message: 'Equipment routes - coming soon' });
});

export default router;